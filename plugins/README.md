# Plugins

OpenClaw plugins installed in this workspace. Symlinks point to the actual
install location managed by `openclaw plugins install`.

## short-turn-router

Symlinked to `/home/pi/.openclaw/extensions/short-turn-router/`. Edits to
the plugin source should happen at the install location (which is what
`openclaw plugins install` manages); this directory just tracks that they
exist.

See `../memory/2026-07-28-short-turn-router.md` for design notes.